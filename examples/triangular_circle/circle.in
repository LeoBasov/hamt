# First test

read triangl_mesh circle_very_fine.msh

set boundary inner 0
set boundary outer 0
set boundary mirrow_left 0 neumann
set boundary mirrow_right 0 neumann

set surface surf q 1

set writer vtk on
set writer vtk file_name circle

run
